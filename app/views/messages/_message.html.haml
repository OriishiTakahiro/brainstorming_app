%div{class: :message}
  %p
    = "#{message.id} #{message.content}"
    %br
    = "投稿者「#{message.contributor}」 得票数 #{message.voted}"
    %br
    = "投稿日時 :#{message.updated_at}"
    %input{ type: :button, value: '投票', id: "#{message.id}", name: :vote }
